name: pina_warehouse
description: Warehouse App Mr. Pina

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^0.4.0+1
  firebase_database: ^3.0.0
  cloud_firestore: ^0.11.0+2
  firebase_auth: ^0.11.1
  cupertino_icons: ^0.1.2
  flutter_screenutil: ^0.5.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/flutter-icon.png